<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/defaults.css"/>
    <style>
        body, html {
            max-width: 100vw;
            overflow-x: hidden;
        }
        
        .game-logo {
            background-color: #f0f0f0;
            border: 1px solid #ddd;
            border-radius: 8px;
            overflow: hidden;
        }
        .game-logo img {
            width: 100%;
            aspect-ratio: 1 / 1;
            object-fit: cover;
        }
        .game-title {
            text-align: center;
            margin-top: 8px;
        }
        
        .container-fluid.m-4 {
            padding-right: var(--bs-gutter-x, .75rem);
            padding-left: var(--bs-gutter-x, .75rem);
            margin-right: auto;
            margin-left: auto;
        }
        
        .row {
            --bs-gutter-x: .75rem;
            margin-right: -var(--bs-gutter-x, .75rem);
            margin-left: -var(--bs-gutter-x, .75rem);
        }


        .row > a {
            color: black;
            text-decoration: none;
        }
        .row > a:hover {
            text-decoration: underline;
        }
        
        /* Landscape orientation adjustments */
        @media (orientation: landscape) {
            .game-container {
                /* Adjust width for 3 items per row, accounting for container margins */
                flex: 0 0 auto;
                width: calc((100% - 3rem) / 3); /* Adjusted for .m-4 margins */
            }
        }

        /* Portrait orientation adjustments */
        @media (orientation: portrait) {
            .game-container {
                /* Ensure full width for portrait mode */
                flex: 0 0 auto;
                width: 100%;
            }

            /* Reduce font size and padding on game title for smaller screens */
            .game-title {
                font-size: 14px;
                margin-top: 4px;
            }
        }

        /* Mobile adjustments */
        @media (max-width: 576px) {
            .game-container {
                /* Further reduce width for smaller screens */
                width: calc(100% - 1.5rem); /* Adjusted for .m-4 margins */
            }

            /* Adjust font size and padding for mobile */
            .game-title {
                font-size: 12px;
                margin-top: 2px;
            }
        }
    </style>
    <title>OmgRod</title>
</head>
<body>
    <%- include('partials/navbar') %> <!-- Ensure your server-side template supports this -->
    <main class="container-fluid m-4">
      <section>
        <h2>Top Compared</h2>
        <div>

        </div>
      </section>
      <section>
        <h2>Highest Rated</h2>
        <div>

        </div>
      </section>
      <section>
        <h2>Most Recent </h2>
        <div>

        </div>
      </section>
    </main>
</body>
</html>